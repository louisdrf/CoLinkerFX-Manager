/*
package com.colinker.database;

import dev.morphia.Datastore;
import dev.morphia.Morphia;

public class MorphiaLocalConfig {
    public static Datastore datastore;
    public static void initializeDatastore() {
        System.out.println("morphia config");
        datastore = Morphia.createDatastore(LocalDatabase.client, LocalDatabase.DATABASE_NAME);
        System.out.println(datastore.getDatabase());
    }
}
*/
